import{createSession,dispatch,runAgent,writeHandoff}from'./runtime.js';
export async function runTaskAnalysisWorkflow(task:string){const sessionId=await createSession(task);const input={taskId:sessionId,title:task,description:task,businessArea:'Finance Operations'};await dispatch(sessionId,'agent-2a-operations-classifier','classify-business-task',input);await dispatch(sessionId,'agent-2b-risk-urgency','assess-finance-risk',input);await runAgent('agent-2a-operations-classifier');await runAgent('agent-2b-risk-urgency');await dispatch(sessionId,'agent-3-recommendation','recommend-next-action',{title:task,category:'Finance Operations',riskLevel:'MEDIUM',urgency:'HIGH',humanReviewRequired:true});await runAgent('agent-3-recommendation');await dispatch(sessionId,'agent-4-guardrail-confidence','hallucination-guard',{originalTask:task,recommendation:{recommendedOwner:'Finance Analyst',recommendedPriority:'HIGH',nextAction:'Verify approval status, confirm accountable owner, and document next action before proceeding.',confidenceScore:0.82},humanReviewRequired:true});await runAgent('agent-4-guardrail-confidence');await writeHandoff(sessionId);return sessionId}
